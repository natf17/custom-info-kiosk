query AboutPage($locale: String!) {
    aboutPage(locale: $locale) {
      pageTitle
      richDescription
      featImg {
        url
        width
        height
        alternativeText
      }
    }
}