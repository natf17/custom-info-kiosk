mutation UpdateError404Page($locale: String!, $input: Error404PagePayload) {
    updateError404Page(locale: $locale, input: $input) {
      pageTitle
      errorDescription
      showRedirectLink
      redirectLink {
        url
        displayText
        description
      }
    }
}