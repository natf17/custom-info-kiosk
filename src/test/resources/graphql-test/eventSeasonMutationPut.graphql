mutation UpdateEventSeason($eventSeasonId: ID, $input: SingleEventSeasonPayload) {
    updateEventSeason(eventSeasonId: $eventSeasonId, input: $input) {
        seasons {
            id
            type
            durationDays
            theme
            serviceYear
            seasonYears
            durationText
        }
       
    }
}
