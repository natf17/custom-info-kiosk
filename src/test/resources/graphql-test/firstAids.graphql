query FirstAids($locale: String, $sort: String) {
    firstAids(locale: $locale, sort: $sort) {
        id
        name
        isWheelchairAccessible
        svgElemId
        note
        location {
            fullname
            level_name
            level_num
        }
        featImg {
            url
            width
            height
        }
    } 
}