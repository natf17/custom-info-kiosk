query GlobalSetting($locale: String) {
    globalSetting(locale: $locale) {
        kioskTitle
        venueName
        locale
        localizations {
            id
            locale
        }
    } 
}