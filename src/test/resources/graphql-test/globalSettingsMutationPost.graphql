mutation CreateGlobalSettings($input: GlobalSettingsPayload!) {
    createGlobalSettings(input: $input) {
        kioskTitle {
            locale
            value
        }
        venueName {
            locale
            value
        }
        localizations
    } 
}