mutation CreateHomePage($locale: String!, $input: HomePagePayload) {
    createHomePage(locale: $locale, input: $input) {
        id
        pageTitle
        tapToContinuePrompt
        welcomeText
        showSelectFromAvailableLocales
    }
}