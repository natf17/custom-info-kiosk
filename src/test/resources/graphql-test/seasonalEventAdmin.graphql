query SeasonalEventAdmin($locale: String, $id: ID!) {
    seasonalEventAdmin(locale: $locale, id: $id) {
        id
        seasonalType
        startDate
        eventLanguage
        event_season {
            serviceYear
            seasonYears
            type
            theme
        }
    }
}