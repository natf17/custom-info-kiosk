mutation CreateSeasonalEvents($eventSeasonId: ID, $input: SeasonalEventPayload) {
    createSeasonalEvents(eventSeasonId: $eventSeasonId, input: $input) {
        id
        seasonalType
        startDate
        eventLanguage
        event_season {
            serviceYear
            seasonYears
            type
            theme
        }
    }
}
